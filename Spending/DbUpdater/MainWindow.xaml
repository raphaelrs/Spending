<Window x:Class="DbUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DbUpdater"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Background="LightBlue">
        <Label x:Name="label" Content="Selecione a base de dados que deseja atualizar&#xD;&#xA;" HorizontalAlignment="Left" Margin="128,77,0,0" VerticalAlignment="Top"/>
        <ComboBox x:Name="comboBox" SelectionChanged="comboBox_SelectionChanged" HorizontalAlignment="Left" Margin="30,124,0,0" VerticalAlignment="Top" Width="454">
            <!--<ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Fill="{Binding Name}" Width="16" Height="16" Margin="0,2,5,2" />
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>-->
        </ComboBox>
        <Button x:Name="button" Content="Atualizar" HorizontalAlignment="Left" Margin="217,165,0,0" VerticalAlignment="Top" Width="75" RenderTransformOrigin="0.302,-0.279" Click="button_Click" IsEnabled="False"/>

    </Grid>
</Window>
